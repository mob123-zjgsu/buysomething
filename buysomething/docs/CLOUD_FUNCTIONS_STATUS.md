# 云函数部署状态

## 当前状态

✅ **register 云函数已成功部署**

| 云函数名称 | 状态 | 运行时 | 部署时间 |
|-----------|------|--------|---------|
| register | Active | Nodejs16.13 | 2026-03-24 23:03:49 |

## 云函数信息

- **函数名称**: register
- **函数ID**: lam-alejr9fz
- **命名空间**: buysomething-6gbmbtpxff05be35
- **运行时环境**: Nodejs16.13
- **状态**: 活跃
- **超时时间**: 60秒

## 功能说明

`register` 云函数实现用户注册功能：

1. 验证手机号格式（11位数字）
2. 验证密码长度（6-20位）
3. 验证验证码（测试环境固定为 123456）
4. 检查手机号是否已注册
5. 创建或更新用户记录
6. 返回用户信息

## 使用方法

在小程序中调用：

```javascript
wx.cloud.callFunction({
  name: 'register',
  data: {
    phone: '13800138000',
    code: '123456',
    password: '123456'
  },
  success: (res) => {
    if (res.result.success) {
      console.log('注册成功:', res.result.userInfo)
    } else {
      console.error('注册失败:', res.result.message)
    }
  },
  fail: (err) => {
    console.error('调用失败:', err)
  }
})
```

## 测试流程

1. 打开小程序
2. 进入"我的"页面
3. 点击"登录/注册"
4. 点击"立即注册"
5. 填写注册信息：
   - 手机号：`13800138000`（或任意11位手机号）
   - 验证码：`123456`（点击"获取验证码"后输入）
   - 密码：`123456`
   - 确认密码：`123456`
   - 勾选用户协议
6. 点击"立即注册"
7. ✅ 注册成功，自动返回登录页

## 云函数控制台

访问云函数管理页面：
[CloudBase云函数控制台](https://tcb.cloud.tencent.com/dev?envId=buysomething-6gbmbtpxff05be35#/scf)

## 数据库集合

注册功能使用以下数据库集合：

- `users` - 存储用户信息 ✅ 已创建

## 注意事项

### 测试环境
- 验证码固定为 `123456`
- 密码明文存储（生产环境需要加密）
- 不会真实发送短信

### 生产环境建议
- 配置腾讯云短信服务
- 使用 bcrypt 加密密码
- 添加验证码过期机制
- 添加手机号发送频率限制

## 故障排查

如果遇到"FunctionName parameter could not be found"错误：

1. 确认云函数已部署（本云函数已部署 ✅）
2. 确认函数名称正确（register）
3. 确认环境ID正确（buysomething-6gbmbtpxff05be35）
4. 等待1-2分钟后重试（云函数需要时间初始化）

## 更新日志

- 2026-03-24 23:03:49 - 初始部署成功
- 2026-03-24 23:26:00 - 添加5个测试用户到数据库
- 2026-03-24 23:30:00 - 创建测试数据文档
